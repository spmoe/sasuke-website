export interface ExpereinceItem {
  thumb: string;
  role: string;
  from: string;
  to: string;
  description: string;
  stack: string;
  url: string;
  category: string;
  hoverThumb?: string;
  isStar?: boolean;
}
